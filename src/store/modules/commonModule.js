const common = {
    state: {
        showBottomSheet: false,
    },
    mutations: {
        toggleBottomSheet(state) {
            state.showBottomSheet = !state.showBottomSheet;
        },
    },
    actions: {
        // Define actions for the module
    },
    getters: {
        // Define getters for the module
    },
};

export default common;
